import React from 'react';

import './flame.scss';

/* eslint-disable-next-line */
export interface FlameProps {}

export const Flame = (props: FlameProps) => {
  return (
    <div>
      <h1>Welcome to flame component!</h1>
    </div>
  );
};

export default Flame;
